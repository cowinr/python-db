# Azure SQL MI demo (demos/azure_sql_mi) and the mssql backend of the
# SQLAlchemy Core demo (demos/sqlalchemy_core).
# Also requires the Microsoft ODBC Driver for SQL Server (17 or 18) on the host.
pyodbc>=4.0

# SQLAlchemy Core demo (demos/sqlalchemy_core)
sqlalchemy>=2.0,<3.0

# Optional backend drivers for the SQLAlchemy Core demo
psycopg2-binary>=2.9    # PostgreSQL
pymysql>=1.1            # MySQL / MariaDB

# Check installed versions like this:
# ❯ pip show sqlalchemy | grep Version
#   Version: 2.0.49