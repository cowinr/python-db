# config.example.py
# ==================
# Copy this file to config.py and fill in your connection details.
#
#   cp config.example.py config.py
#
# config.py is gitignored — never commit it.

SERVER   = "your-instance.xxxxxxxxxxxxxxxx.database.windows.net"
DATABASE = "your_database_name"

# Optional: override the default ODBC driver (see defaults.py for the default).
# ODBC_DRIVER = "ODBC Driver 18 for SQL Server"
